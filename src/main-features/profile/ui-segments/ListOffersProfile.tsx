import React from 'react';
import {IOffer} from "../../../shared/model/offer.model";

interface IOfferList {
    offerList: IOffer[];
}
export function ListOffersProfile(props: IOfferList) {
    const {offerList} = props;
    return (
        <div>fsdf</div>
    );
}